# I18n

The demo form uses Formspec Locale sidecars loaded into the active engine.

## Locale Sidecars

`src/demo/locales.ts` publishes English and Spanish Locale Documents for the demo definition. The documents include:

- `$form.title`.
- `$form.description`.
- Field labels and hints.
- Option-set labels.

## Locale Toggle

The respondent shell reads the locale list from `x-formspec-locales` metadata and calls `engine.setLocale(code)` when the respondent switches language.

The React renderer updates labels through engine locale signals. The shell also re-resolves the form title and description after switching.

## FEL Interpolation

Locale strings can include Formspec Expression Language interpolation. The demo includes an interpolated preferred-language hint that reads from `$applicant.fullName`, proving localized strings can react to response data.

## Production Runtime Gap

M6 proves Locale sidecar loading with the bundled demo definition. The current
`formspec-server` runtime payload exposes `locale_refs`, but it does not yet
serve concrete Locale Documents for the web runtime to load. Production
sidecar localization therefore remains a follow-up integration gap unless the
adopter publishes inline Locale Documents through the runtime payload.
