services:
  formspec-web:
    build: .
    image: formspec-web:local
    ports:
      - "8080:80"
    environment:
      FORMSPEC_WEB_PROFILE: publicPortal
      FORMSPEC_WEB_SERVER_URL: ${FORMSPEC_WEB_SERVER_URL:-}

  formspec-web-department:
    image: formspec-web:local
    depends_on:
      - formspec-web
    ports:
      - "8081:80"
    environment:
      FORMSPEC_WEB_PROFILE: departmentApp
      FORMSPEC_WEB_SERVER_URL: ${FORMSPEC_WEB_SERVER_URL:-}
