# FW-0040 — Embed: form lives in the host's page (implementation plan)

**Date:** 2026-05-24
**Row:** [FW-0040](../../PLANNING.md#fw-0040--embed-form-lives-in-the-hosts-page)
**Design:** [`thoughts/specs/2026-05-24-fw-0040-embed-design.md`](../specs/2026-05-24-fw-0040-embed-design.md)
**Subordinate to:** web ADR-0009, web ADR-0011, stack-root [ADR-0128 (frontend surface architecture)](../../../thoughts/adr/0128-frontend-surface-architecture.md)
**Precedent (execution shape):** FW-0027 multi-rail payment + FW-0044 offline-capable fill + FW-0033 file upload — IN-FORM port + capability-key extension; backfill all policy-resolution fixtures in a single commit; runtime gate at form load.

## Execution shape

Slice 1 lands in five commits (TDD red/green/refactor per commit family). Each commit uses explicit-paths `git commit <path> -m` form per `formspec-stack/CLAUDE.md` §"Parallel-agent commit safety." Commit boundaries align with the policy-fixture / port / runtime-integration / docs / planning split that the FW-0027 + FW-0044 + FW-0033 sliders all used.

**Coordination with parallel work:** FW-0027 implementer is running in parallel and ALSO adding a runtime-feature key (`payment`). The append-only protocol handles both orderings. Verify the current state of `src/policy/feature-keys.ts` immediately before the policy commit; if `payment` is already at the tail, append `embed` as the **8th** key. If `payment` is not yet landed, append `embed` as the **7th** key. The PR's policy-resolution fixtures must match the actual taxonomy state at commit time.

Uncommitted FW-0027 work in `src/app/RespondentRuntime.tsx`, `src/app/respondent-flow.ts`, `tests/app/respondent-flow.test.ts`, `tests/app/respondent-runtime-payment.test.tsx` (per `git status` at task start) MUST NOT be captured by FW-0040 commits — use explicit-paths `git commit <path> -m` form exclusively.

## Commit 1 — Design + plan (docs only)

**Files:**
- `thoughts/specs/2026-05-24-fw-0040-embed-design.md` (new)
- `thoughts/plans/2026-05-24-fw-0040-embed.md` (new — this file)

**Commit message:**
```
docs(fw-0040): design + plan for slice-1 embed (form-in-host-page) substrate
```

## Commit 2 — Port + types + adapters + conformance suite + stub tests

**Files (red/green/refactor per file family):**
- `src/ports/embed-transport.ts` (new) — `EmbedTransport` interface + `EmbedMessage` + `EmbedMessageFromHost` + `Unsubscribe` types.
- `src/ports/index.ts` (modify) — re-export.
- `src/adapters/stub/embed-transport.ts` (new) — `stubEmbedTransport({embedded?, hostOrigin?})`, marked `DEMO_STUB_ADAPTER`, exposes `_internalSentMessages()` + `_internalSimulateHostMessage()`.
- `src/adapters/unavailable/embed-transport.ts` (new) — `unavailableEmbedTransport()`, marked `UNAVAILABLE_ADAPTER`.
- `src/adapter-conformance/conformance.ts` (modify) — `defineEmbedTransportConformance` + `EmbedTransportConformanceSubject`.
- `src/adapter-conformance/index.ts` (modify) — re-export.
- `src/adapter-conformance/fixtures.ts` (modify) — `sampleAllowedHostOrigin` (`'https://allowed.example.test'`) + `sampleEmbedMessage` (host-handshake).
- `tests/adapter-conformance/_framework/conformance.ts` (modify) — re-export.
- `tests/adapter-conformance/embed-transport/conformance.test.ts` (new) — runs suite against stub.
- `tests/adapters/embed-transport-stub.test.ts` (new) — stub-specific.
- `tests/adapters/embed-transport-unavailable.test.ts` (new) — sentinel-specific.
- `tests/adapters/unavailable-sentinel.test.ts` (modify) — extend with new sentinel.
- `tests/adapters/demo-stub-marker.test.ts` (modify) — extend with new stub.
- `scripts/check-conformance-coverage.mjs` (modify) — register `EmbedTransport`, `src/adapters/stub/embed-transport.ts`, `src/adapters/unavailable/embed-transport.ts`, `defineEmbedTransportConformance`.
- `tests/adapter-conformance/README.md` (modify) — list new port suite.

**Verification:**
```sh
npm run test:unit -- adapter-conformance/embed-transport
npm run test:unit -- adapters/embed-transport
npm run test:unit -- adapters/unavailable-sentinel
npm run test:unit -- adapters/demo-stub-marker
node scripts/check-conformance-coverage.mjs
```

**Commit message:**
```
feat(fw-0040): EmbedTransport port + stub + unavailable + conformance
```

## Commit 3 — Policy: feature key + port map + extractor + error class + resolver limits + composition wiring

**Files:**
- `src/policy/feature-keys.ts` (modify) — append `'embed'` after the current tail (verify state). Update doc comment.
- `src/policy/feature-keys.test.ts` (modify) — extend `RUNTIME_FEATURE_KEYS.toEqual(...)`; assert `isRuntimeFeatureKey('embed') === true`; extend locale-conditional assertions.
- `src/policy/feature-port-map.ts` (modify) — add `embed: 'embedTransport'`.
- `src/policy/policy-shapes.ts` (modify) — add `EmbedLimits` type + comment describing the `limits.embed` shape.
- `src/policy/errors.ts` (modify) — add `EmbedOriginNotAllowedError extends RuntimePolicyError` with code `'EmbedOriginNotAllowed'`; extend `RuntimePolicyErrorCode` union.
- `src/policy/errors.test.ts` (modify) — extend.
- `src/policy/resolver.ts` (modify) — extend `validateInput` to walk `limits.embed.allowedOrigins`; throw `InvalidRuntimePolicyError` on malformed entries.
- `src/policy/resolver.test.ts` (modify) — cover the new validation branch.
- `src/policy/extract-form-policy.ts` (modify) — add `extractEmbeddableOptIn(definition)` walker; returns `'optional' | undefined`.
- `src/policy/extract-form-policy.test.ts` (modify) — add absent / present-true / present-false / present-non-boolean cases.
- `src/adapters/composing/form-runtime-policy-extractor.ts` (modify) — add `EmbeddableExtractor`.
- `src/composition/types.ts` (modify) — add `embedTransport: EmbedTransport` slot + doc comment.
- `src/composition/default.ts` (modify) — declare `embed: 'unavailable'` + wire `unavailableEmbedTransport()` + add `limits.embed: { allowedOrigins: [] }` + append `new EmbeddableExtractor()` to composite; update org-policy features.
- `src/composition/stub.ts` (modify) — declare `embed: 'demo-stub'` + wire `stubEmbedTransport({ embedded: false })` + add `limits.embed: { allowedOrigins: [] }` + append extractor; update org-policy.
- `src/composition/route-narrowing.ts` (modify) — add `embedTransport: unavailableEmbedTransport()` in both branches; extend `instanceCapabilities.embed = 'unavailable'` + `orgRuntimePolicy.features.embed = 'allowed'` + `limits.embed: { allowedOrigins: [] }` in both branches.
- `src/policy/freeze-composition.test.ts` (modify) — extend coherent + incoherent cases with new slot.
- `tests/profiles/composition-coherence.test.ts` (modify if it iterates ports).
- `tests/composition/route-narrowing.test.ts` (modify) — descriptor matrix; assert `'embed'` is not in any descriptor's `consumes` set today.
- `tests/policy-resolution/cases/embed-optional-allowed.json` (new).
- `tests/policy-resolution/cases/embed-disabled-no-instance.json` (new).
- `tests/policy-resolution/cases/embed-org-forbidden.json` (new).
- `tests/policy-resolution/cases/*.json` (modify pre-existing 30+ cases) — append `embed: 'unavailable'` to `instance` + `embed: 'allowed'` to `org.features` per the append-only protocol; for cases asserting `expect.disabled`, append `embed: 'not-requested'` only where the case opts every key into `disabled`.

**Verification:**
```sh
npm run test:unit -- policy
npm run test:unit -- policy-resolution
npm run test:unit -- composition
npm run test:unit -- profiles
```

**Commit message:**
```
feat(fw-0040): embed runtime-feature key + extractor + composition wiring
```

## Commit 4 — Runtime integration + RespondentRuntime tests

**Files:**
- `src/app/respondent-flow.ts` (modify) — add `verifyEmbedOriginAllowed(runtimeProfile, embedTransport, orgRuntimePolicy)` pure helper + `matchesAllowedOrigin(hostOrigin, allowed)` helper.
- `src/app/respondent-flow.test.ts` (modify) — unit-test decision matrix (not embedded; allowed; disallowed; wildcard; null hostOrigin; malformed allow-list).
- `src/app/RespondentRuntime.tsx` (modify) — call `verifyEmbedOriginAllowed(...)` inside `createReadyState` after `resolveRuntimeFeatures(...)`. Extend `runtimePolicyErrorCopy` to handle `EmbedOriginNotAllowedError` (fixture-pinned "this form is not set up to be shown on this site." copy).
- `tests/app/respondent-runtime-embed.test.tsx` (new) — end-to-end:
  - top-level (not embedded) + embeddable form → mounts.
  - embedded + allowed origin → mounts.
  - embedded + disallowed origin → error page + fixture copy.
  - embedded + wildcard `['*']` allow → mounts.
  - embedded + null hostOrigin (stub configured with `hostOrigin: undefined`) → error page (fail-closed).
  - vocabulary firewall: rendered DOM never contains `iframe`, `postMessage`, `EmbedTransport`, `allowedOrigins`, `hostOrigin`, `embed-transport`, `embedTransport` substrings.

**Verification:**
```sh
npm run test:unit -- app/respondent-flow
npm run test:unit -- app/respondent-runtime-embed
npm run test:unit -- app/respondent-runtime
```

**Commit message:**
```
feat(fw-0040): RespondentRuntime iframe-context gate + EmbedOriginNotAllowed
```

## Commit 5 — Docs + ADR cross-ref + PLANNING + follow-ons

**Files:**
- `docs/ports/embed-transport.md` (new) — adopter doc per the other-port template. Names FW-0053 as the Custom Element wrapper, FW-0102 as the production transport adapter row; documents the `allowedEmbedOrigins` matcher semantics + the `'*'` warning; documents the production-adapter handshake pattern.
- `docs/policy/runtime-feature-resolution.md` (modify) — add `embed` to the feature-keys list + a worked example "the in-form iframe-origin gate (FW-0040 slice 1)".
- `thoughts/adr/0011-runtime-feature-resolution-and-policy-gates.md` (modify) — footer Related Decisions bullet for the embed slice; ratify that the §"Instance capabilities" + §"Feature Ownership Table" `embed` row is now materialized.
- `PLANNING.md` (modify) — FW-0040 row → `live (slice 1)` with named release gaps. Add FW-0053 cross-reference. Add follow-on rows FW-0102 / FW-0103 / FW-0104 / FW-0105 / FW-0106.

**Verification:**
```sh
npm run ci
```

**Commit message:**
```
docs(fw-0040): adopter doc + ADR-0011 cross-ref + PLANNING closeout
```

## Discipline reminders

- **No shims.** No backwards-compat aliases; new port + new key.
- **No comments unless WHY is non-obvious.** TS types document themselves; JSDoc only where the invariant is load-bearing.
- **Vocabulary firewall.** Spec jargon stays internal; user-facing copy uses plain "embed" / "this form is shown on" / "not set up to be shown."
- **Explicit-paths commits.** `git commit <path> -m` form, heredoc message.
- **Verify each commit.** Run the named verification commands; fix red before continuing.
- **No `git add .`** — FW-0027 sibling work is in the index; explicit paths only.
- **Review-in-loop.** Inline arch-review skill invocation on the slice; flag in return for independent dispatch.
