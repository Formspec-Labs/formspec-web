# Specs — formspec-web

Design exploration documents for the public reference UI. Named `YYYY-MM-DD-kebab-slug.md`. Loose structure: problem → design → open questions. Often the output of brainstorming for an `FW-*` cluster.
